# ============================================================
# Vigil — Stack de Adquisición y Exploración de Datos
# Equipo: RndmStudio (este equipo)
# Rol: Descarga, normalización, exploración de datos raw
# Instalar con: pip install -r requirements.txt
# ============================================================

# --- Gemini API (clasificación ligera, OCR de PDFs) ---
google-genai>=1.0.0

# --- ETL / Core ---
polars>=1.0.0
pyarrow>=16.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
orjson>=3.9.0

# --- Descarga de datos ---
httpx>=0.27.0
requests>=2.31.0
tqdm>=4.66.0

# --- Scraping ---
apify-client>=1.7.0

# --- Geoespacial ---
geopandas>=0.14.0
folium>=0.17.0
shapely>=2.0.0

# --- Exploración / SNA ---
networkx>=3.3
python-louvain>=0.16
duckdb>=1.0.0

# --- Calidad de datos ---
pandera>=0.19.0

# --- Configuración ---
jupyter>=1.1.0
ipykernel>=6.29.0

# ============================================================
# Diferidas — Equipo SAEIL (NO instalar aquí)
# ============================================================
# sentence-transformers  → embeddings (SAEIL)
# pysentimiento          → sentimiento (SAEIL)
# transformers           → modelos HF (SAEIL)
# scikit-learn           → ML (SAEIL)
# neo4j                  → grafo producción (SAEIL)
# google-cloud-bigquery  → BigQuery (SAEIL)
# google-cloud-storage   → GCS upload (SAEIL)
# toolbox-core           → MCP Neo4j (SAEIL)
# notebooklm-mcp-cli     → descomentar si se activa integración NotebookLM
